// Valid and invalid variable names

// var thisIsFine; // Pascal case
// var $noProblemHere; // May start with $
// var _underscore56; // Should not start with underscore
// var StartWithCapital; // Camel case / title case
// var z5; // Can contain alpha / numerics

// var 999; // Should not br a only numeric value
// var 39Steps; // Shoun't start with numbers
// var &nope; // Shouldn't start with &
// var single words only; // shoulls not contain spaces
// var yield; // Should not have default JS syntax (i.e., if, break, var, const)var thisIsFine; // Pascal case


var $noProblemHere; // May start with $
var underscore56; // Should not start with underscore
var StartWithCapital; // Camel case / title case
var z5; // Can contain alpha / numerics

var z999; // Should not br a only numeric value
var a39Steps; // Shoun't start with numbers
var nope; // Shouldn't start with &
var single, words, only; // shoulls not contain spaces
var yields; // Should not have default JS syntax (i.e., if, break, var, const)



/* Further Adventures
 *
 * 1) Take a look at the errors in the
 *    error section of Console below.
 *
 * 2) Click Run in the console
 *    and see what errors appear there.
 *
 * 3) Delete the second block of variables
 *    one by one from the code.
 *    See how the errors change.
 *
 */